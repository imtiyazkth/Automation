org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# ---------------------------------------------------------------------------
# Secrets — DO NOT COMMIT REAL VALUES. Populate via local.properties or CI
# secrets instead, and read them through BuildConfig fields (see app/build.gradle.kts).
# GEMINI_API_KEY=
# WHATSAPP_CLOUD_API_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
# TELEGRAM_BOT_TOKEN=
# ---------------------------------------------------------------------------
